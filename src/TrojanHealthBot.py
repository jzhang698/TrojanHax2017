class TrojanHealthBot:
    weights = []
    bias = 0
        
    def train(filename):
        trainFile = open(filename)
        print(trainFile.read())

    def test(filename):
        testFile = open(filename)
    
    def __init__(self, numFeatures, trn, tst):
        weights = [0] * numFeatures
        train(trn)
        test(tst)
